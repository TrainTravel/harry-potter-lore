FROM python:3.11-slim

WORKDIR /app
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY context_harness/ ./context_harness/
COPY data/             ./data/

EXPOSE 8000
CMD ["uvicorn", "context_harness.app:app", "--host", "0.0.0.0", "--port", "8000"]
